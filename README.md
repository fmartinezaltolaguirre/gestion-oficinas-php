# Gestion de Oficinas PHP

## Instalacion

```bash
cp .env.example .env
composer install
composer serve
```

Abrir `http://127.0.0.1:8080`.

Completar `.env` con los datos de Entra ID, SharePoint y los IDs de las listas. Los campos `Title` y `Codigo` son provisionales.
